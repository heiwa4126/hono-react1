{
	"name": "hono-react1",
	"version": "1.0.0",
	"description": "",
	"keywords": [],
	"license": "ISC",
	"author": "",
	"type": "module",
	"main": "index.js",
	"scripts": {
		"format": "oxfmt",
		"lint": "oxlint",
		"check": "oxfmt && oxlint --fix",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"dependencies": {
		"hono": "^4.12.18"
	},
	"devEngines": {
		"packageManager": {
			"name": "pnpm",
			"version": "^11.1.0",
			"onFail": "download"
		}
	}
}
