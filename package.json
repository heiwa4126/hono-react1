{
	"name": "hono-react1",
	"version": "1.0.0",
	"description": "",
	"keywords": [],
	"license": "ISC",
	"author": "",
	"type": "module",
	"main": "index.js",
	"scripts": {
		"format": "oxfmt",
		"lint": "oxlint",
		"check": "oxfmt && oxlint --fix",
		"workspace:install": "pnpm -r install",
		"workspace:update": "pnpm -r update",
		"build": "pnpm -r build",
		"start": "pnpm --dir apps/api start",
		"workspace:list": "pnpm list -r --depth -1",
		"workspace:path": "pnpm -r exec pwd",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"dependencies": {
		"hono": "^4.12.18",
		"zod": "^4.4.3"
	},
	"devEngines": {
		"packageManager": {
			"name": "pnpm",
			"version": "^11.1.0",
			"onFail": "download"
		}
	}
}
